# Port the service listens on (Railway sets this automatically)
PORT=3001

# Secret token — must match BROWSER_SERVICE_SECRET in the main Velamini Vercel environment
# Generate with: openssl rand -hex 32
BROWSER_SERVICE_SECRET=your-secret-here
